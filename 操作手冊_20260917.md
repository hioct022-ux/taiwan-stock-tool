# 2026-09-17 操作手冊

> 每一步都有：**要做什麼 → 預期看到 → 看到這個代表出事 → 出事怎麼辦**
>
> 任何一步看到不在預期內的畫面，**先停下來，把畫面貼給 AI**，不要硬著頭皮往下做。
>
> 全程約 15 分鐘（其中 6 分鐘是等程式跑）。

---

## 步驟 0：先脫離 `quote>` 卡死狀態

**做什麼：** 如果你的終端機現在顯示 `quote>`，按 **`Ctrl + C`**。

**預期看到：** 回到正常提示字元，長得像這樣（結尾是 `%` 或 `$`）：

```
chenbingyang@MacBook 台股分析工具 %
```

**出事的樣子：** 還是 `quote>` → 再按一次 `Ctrl + C`。還是不行就關掉終端機視窗重開一個。

**為什麼會卡：** 從聊天視窗複製的文字，雙引號 `"` 可能被轉成彎引號 `"`，zsh 不認得、以為引號沒關完。所以這份手冊的指令**全部不含引號**。

---

## 步驟 1：切到專案資料夾

**做什麼：** 貼這一行，按 Enter。

```
cd ~/台股分析工具
```

**預期看到：** 什麼訊息都沒有，直接跳到下一行提示字元。**沒消息就是好消息。**

**出事的樣子：**
```
cd: no such file or directory: /Users/chenbingyang/台股分析工具
```
→ 資料夾路徑不對，停下來問。

---

## 步驟 2：關掉正在跑的 Streamlit

**做什麼：**

```
pkill -f streamlit
```

**預期看到：** 兩種都正常
- 沒有任何訊息 → App 有在跑，已經被關掉
- 沒有任何訊息（App 本來就沒開）→ 也一樣沒訊息

**為什麼要做：** Streamlit 開著會佔住 SQLite 資料庫的鎖，後面寫入資料會失敗。

**出事的樣子：** 幾乎不會出事。這個指令找不到程式也不會報錯。

---

## 步驟 3：備份資料庫

**做什麼：**

```
cp data/stock.db data/stock.db.bak-20260917
```

**預期看到：** 沒有訊息 = 成功。

**確認一下有沒有真的備份到：**

```
ls -lh data/stock.db.bak-20260917
```

預期看到類似：
```
-rw-r--r--  1 chenbingyang  staff   85M  9 17 22:10 data/stock.db.bak-20260917
```
**重點是檔案大小要是幾十 MB，不能是 0B。**

**出事的樣子：**
```
ls: data/stock.db.bak-20260917: No such file or directory
```
→ 備份沒成功，**不要往下做**，停下來問。

---

## 步驟 4：回填「最初進場日」— 先試跑

**要修什麼：** 你的六筆持倉，因為「續抱」會把進場日重設，導致畫面上的持有天數只算本輪（微星實際抱了 6 週，畫面顯示第 3 天）。這一步把真正的最初買進日推估回來。

**做什麼：**

```
python3 repair_original_entry_date.py
```

**⚠️ 這一步不會寫入任何東西，只會印報告。可以放心跑。**

**預期看到：** 大概長這樣（數字會不一樣）

```
共 6 筆持倉　模式：試跑（不寫入，加 --apply 才會寫）
============================================================================
  #1 3231 緯創  [closed] 續抱1次
       本輪起算 2026-08-13　進場價 169.5
       → 推估原始進場日 **2026-07-30**　累計 11 個交易日起算
         依據：理論值 2026-07-30（往前 10 個交易日）；進場價正好落在該日高低區間內 ✅
  ...（其餘幾筆）

※ 確認上面的推估合理後，執行：python3 repair_original_entry_date.py --apply
```

**你要檢查什麼：** 看每一筆最後那行「依據」。
- 出現 **✅ 區間相符** → 兩個獨立方法互相印證，可信
- 出現 **⚠️ 附近找不到能容納 XXX 的交易日** → 那筆推估較不可靠，記下來，之後可以在 App 的持倉列用 ✏️ 手動改

**六筆的參考答案**（CLAUDE.md 陷阱47 記的，應該要對得起來）：

| 股票 | 推估的最初進場日 |
|---|---|
| 緯創 | 2026-07-30 |
| 微星（續抱2） | 2026-07-17 |
| 國泰金 | 2026-07-31 |
| 微星（續抱3） | 2026-07-22 |
| 仁寶 | 2026-08-21 |
| 所羅門 | 2026-08-21 |

**出事的樣子：**
```
ModuleNotFoundError: No module named 'database'
```
→ 你不在專案資料夾裡，回步驟 1。

```
sqlite3.OperationalError: database is locked
```
→ Streamlit 還開著，回步驟 2。

---

## 步驟 5：回填「最初進場日」— 實際寫入

**做什麼：** 上一步的推估看起來合理才做這步。

```
python3 repair_original_entry_date.py --apply
```

**預期看到：** 跟上一步一樣的報告，但最後多兩行：

```
✅ 已寫入 5 筆
※ 推估值可能不精確——持倉列的 ✏️ 編輯表單可手動修正原始進場日。
```

**寫入 5 筆是正常的**（有一筆本來就有值，會顯示「已有 original_entry_date=...，略過」）。

**出事的樣子：**
```
sqlite3.OperationalError: database is locked
```
→ Streamlit 還開著，回步驟 2 再重跑這一步。**重跑是安全的**，已經寫過的會自動略過。

---

## 步驟 6：把程式改動存進 git

**要做什麼：** 今天改了 App 的文字（那個叫你提早減碼的框），要存檔並推到 GitHub，雲端版才會跟著更新。

### 6-1 先清鎖（保險起見）

```
rm -f .git/index.lock .git/HEAD.lock
```

**預期看到：** 沒訊息 = 成功（檔案不存在也不會報錯）。

### 6-2 把檔案加進待提交清單

```
git add app.py database.py fetcher.py backtest_stocks.py CLAUDE.md backtest_market_score.py backtest_portfolio_slots.py repair_original_entry_date.py backfill_chips_history.py 操作手冊_20260917.md
```

**預期看到：** 沒訊息 = 成功。

**出事的樣子：**
```
fatal: pathspec 'xxx.py' did not match any files
```
→ 某個檔名打錯或檔案不存在。把完整錯誤貼給我。

### 6-3 確認要提交什麼（建議做，確認沒多加東西）

```
git status --short
```

**預期看到：** 開頭是 `M` 或 `A` 的那幾行，就是要提交的：

```
M  CLAUDE.md
M  app.py
M  backtest_stocks.py
M  database.py
M  fetcher.py
A  backfill_chips_history.py
A  backtest_market_score.py
A  backtest_portfolio_slots.py
A  repair_original_entry_date.py
A  操作手冊_20260917.md
```

下面可能還有一堆 `M 台股分析工具_backup/...` 和 `?? data/...`——**那些前面沒有變綠、沒被 add 進去，不會被提交，可以不管**。

### 6-4 提交

```
git commit -m fix-ui-advice-vs-strategy-c
```

**注意：訊息刻意用純英文、不加引號**，避免又卡 `quote>`。

**預期看到：**
```
[main a1b2c3d] fix-ui-advice-vs-strategy-c
 10 files changed, 350 insertions(+), 45 deletions(-)
```
**重點是有出現 `files changed`。**

**出事的樣子：**
```
nothing to commit, working tree clean
```
→ 6-2 沒成功，回去重做。

```
Another git process seems to be running
```
→ 回 6-1 清鎖。

### 6-5 推上 GitHub

```
git push origin main
```

**預期看到：**
```
Enumerating objects: 25, done.
...
To https://github.com/hioct022-ux/taiwan-stock-tool.git
   9b7ba44..a1b2c3d  main -> main
```
**重點是最後那行有 `main -> main`。**

**出事的樣子：**

| 錯誤訊息 | 意思 | 怎麼辦 |
|---|---|---|
| `Invalid username or token` | GitHub 認證過期 | 見 CLAUDE.md 陷阱16 |
| `Internal Server Error` | 本機倉庫有垃圾檔 | 跑 `git fsck --full`，見陷阱37 |
| `rejected ... non-fast-forward` | 遠端有你沒有的東西 | **停下來問，不要自己 force push** |

### 6-6 確認真的推上去了

```
git log --oneline -1
git ls-remote origin HEAD
```

**預期看到：** 兩行輸出的**開頭七碼要一樣**，例如都是 `a1b2c3d`。不一樣代表 push 沒真的成功（陷阱17）。

---

## 步驟 7：補籌碼歷史 — 先試跑

**要補什麼：** 回測期間有 159 個交易日完全沒有法人買賣超資料，導致那些日子的評分只有技術面在算。這一步把它補回來。

**做什麼：**

```
python3 backfill_chips_history.py
```

**⚠️ 這一步不連網、不寫入，只列出缺哪幾天。**

**預期看到：**
```
起點 2025-07-01　交易日 299 天　chips 已有 140 天
需要補 **159 天**　預估耗時 5.3 分鐘
模式：試跑（不抓不寫，加 --apply 才會執行）
========================================================================
缺漏的交易日：
  2025-07-01  2025-07-02  2025-07-03  ...
  ...
※ 確認無誤後執行：python3 backfill_chips_history.py --apply
```

**你要檢查什麼：** 缺漏日期應該是 **2025-07 到 2026-02 的一整段連續交易日**。如果看到的是零星幾天散落各處，代表狀況跟預期不同，停下來問。

---

## 步驟 8：補籌碼歷史 — 實際抓取（要等 5～6 分鐘）

**做什麼：**

```
python3 backfill_chips_history.py --apply
```

**⚠️ 這一步會連上證交所抓 159 次資料，每次間隔 2 秒。過程中畫面會一行一行慢慢跑，這是正常的，不要中斷。**

**預期看到（跑的過程）：**
```
  [1/159] 2025-07-01　✅ 1243 筆
  [2/159] 2025-07-02　✅ 1251 筆
  [3/159] 2025-07-03　✅ 1248 筆
  ...
```

每行結尾的「筆數」應該是 **1000～1500 之間**（全上市股）。

**也可能看到這幾種，都不算失敗：**

| 畫面 | 意思 | 要不要緊 |
|---|---|---|
| `⚪ 查無資料，跳過` | 那天 TWSE 沒有 T86（假日或太久遠） | 不要緊 |
| `⚠️ API 回傳的是 2026-09-16，本日未補到` | 證交所忽略了日期參數 | 那天沒補到，但**不會寫錯資料** |
| `❌ ReadTimeout` | 單次連線逾時 | 那天沒補到，跑完可以再跑一次補 |

**預期看到（跑完的結尾）：**
```
========================================================================
補齊 159 天　查無資料 0 天　日期不符 0 天　失敗 0 天
重算後仍缺 0 天 ✅
自選股籌碼涵蓋：上市 70 檔有資料；上櫃 17 檔永遠沒有（T86 不含上櫃）

※ 下一步：重建回測快取才會用到新資料
   python3 backtest_portfolio_slots.py cache
```

**這幾個數字的意思：**
- **補齊 N 天** ← 這個越接近 159 越好
- **查無資料 N 天** ← 如果這個很大（例如 100 以上），代表證交所不提供那麼久以前的資料，**這不是你的錯，也不是程式壞了**，把數字告訴我，我重新評估
- **上櫃 17 檔永遠沒有** ← 這行是預期內的，不是錯誤

**出事的樣子：**

| 錯誤 | 怎麼辦 |
|---|---|
| 全部都是 `❌` | 網路或證交所擋了，等一下再跑 |
| 跑到一半當掉 | **直接再跑一次同樣的指令**，它會重新算缺口、只補還沒補的 |
| `database is locked` | Streamlit 又開著了，回步驟 2 |

---

## 步驟 9：重建回測快取（要等幾分鐘）

**做什麼：**

```
python3 backtest_portfolio_slots.py cache
```

**為什麼要做：** 回測用的是預先算好的快取。不重建的話，補進去的籌碼資料不會被用到。

**預期看到：**
```
  [1/87] 2049 上銀　230 個評分點
  [2/87] 2408 南亞科　225 個評分點
  ...
  [87/87] 2327 國巨　220 個評分點

✅ 快取已存 /tmp/_pf_slots_cache.pkl（74 檔）
```

**出事的樣子：** 跑到一半停住不動超過 10 分鐘 → `Ctrl + C` 中斷，把畫面貼給我。

---

## 步驟 10：重開 App

```
streamlit run app.py
```

**預期看到：**
```
  You can now view your Streamlit app in your browser.
  Local URL: http://localhost:8501
```
瀏覽器會自動打開。

**⚠️ 一定要重開，不能只是重新整理網頁。** 舊的快取存在程式記憶體裡，不重開不會清掉（陷阱40）。

**重開後去大盤分析頁確認：** 綜合判斷框如果是偏空，應該會看到新加的這段：

```
📌 已持有的部位：不因這個分數出場。
   正式規則（策略C）的出場只有兩項——
   ① 觸及停損單  ② 滿 10 個交易日且評分不足
```

看到這段 = 今天的 UI 修正生效了。

---

## 全部做完之後

跟 AI 說一聲，並附上**步驟 8 結尾那四個數字**（補齊/查無資料/日期不符/失敗）。

我會用補好的資料重跑一次「65 分門檻到底有沒有用」的驗證，看補上籌碼那 35% 權重之後，結論有沒有改變。

---

## 任何一步不確定時

**把終端機畫面整段複製貼給 AI。** 不要猜、不要硬做下一步。

特別是這幾種情況一定要先問：
- 出現任何紅色的 `fatal:` 或 `error:`
- 出現 `rejected` 或 `non-fast-forward`
- 數字跟這份手冊寫的差很多
