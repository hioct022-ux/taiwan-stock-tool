# 換裝置 / 備份還原說明

## 資料夾備份

直接複製整個「台股分析工具」資料夾即可，包含：
- 所有程式碼
- SQLite 資料庫（data/stock.db）
- 歷史 JSON 資料（data/json/）
- config_local.py（Token 設定）

---

## 換裝置後的三個額外步驟

### 1. 安裝 Python 套件

```bash
cd ~/台股分析工具
pip3 install -r requirements.txt
```

### 2. 安裝自動排程

每日 16:30 自動抓資料的排程不在資料夾內，需重新安裝：

```bash
cd ~/台股分析工具
bash install_autostart.sh
```

### 3. 設定 git 推送認證

git 的 SSH key 或 macOS Keychain 認證不會隨資料夾轉移，需重新設定才能推送到 GitHub：

```bash
# 確認 remote 是否正確
git -C ~/台股分析工具 remote -v

# 若使用 SSH，需在新裝置上產生 SSH key 並加到 GitHub 帳號
ssh-keygen -t ed25519 -C "your_email@example.com"
# 將 ~/.ssh/id_ed25519.pub 內容加到 GitHub → Settings → SSH Keys
```

---

## 驗證是否正常

```bash
# 測試程式能執行
cd ~/台股分析工具
python3 -c "from database import init_db; init_db()"

# 測試資料抓取
python3 -c "from fetcher import fetch_all; fetch_all()"

# 啟動 Streamlit
bash run.sh
```
